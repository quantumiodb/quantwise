# macOS Tools

macOS productivity utilities for Claude Code.

## Commands

| Command | Description |
|---------|-------------|
| `/listen` | Activate macOS speech recognition (dictation) |
| `/cherry-pick-resolve` | Interactively resolve git cherry-pick conflicts |

## Requirements

- macOS with Dictation enabled (System Settings > Keyboard > Dictation)
- Terminal app granted Accessibility permissions
