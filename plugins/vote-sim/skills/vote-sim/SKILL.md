---
name: vote-sim
description: "群体决策模拟器。用正态分布温度控制N个独立LLM agent投票，模拟人类群体对任意问题的意见分布。适用于技术选型、产品决策、生活选择、政策讨论等。用法: /vote-sim \"问题\" 选项1 选项2 [选项3...]"
user-invocable: true
allowed-tools: [Bash]
context: inline
argument-hint: '"决策问题" 选项1 选项2 [选项3...] [--n=100] [--mean=0.6] [--std=0.20] [--context="..."]'
---

## 群体决策模拟器 — Vote Simulator

用正态分布温度（μ=0.6, σ=0.20）控制多个独立 LLM agent 投票，模拟不同理性程度的人群对同一问题的意见分布。温度低 → 理性坚定，温度高 → 情绪化易偏离立场。

**适用场景**：技术选型、产品功能优先级、团队决策、生活选择、政策讨论、任何多选项问题。

### 参数

```
$ARGUMENTS
```

解析规则：
- 第一个参数（双引号包裹）= 投票问题
- 后续空格分隔的词 = 投票选项（支持 2 个及以上）
- 可选 flags: `--n=<数量>` `--mean=<温度均值>` `--std=<标准差>` `--concurrency=<并发数>` `--context=<背景信息>`

示例:
- `/vote-sim "下一个功能优先级" 性能优化 新功能 技术债务清理 --n=60 --context="团队5人，产品成长期"`
- `/vote-sim "新项目用哪个前端框架" React Vue Svelte --n=50`
- `/vote-sim "周末活动怎么安排" 爬山 海边 宅家看剧 --n=30`
- `/vote-sim "应该先做哪个" A功能 B功能 C功能 --n=40 --context="用户反馈数据"`
- `/vote-sim "是否支持这个提案" 支持 反对 弃权 --n=100 --mean=0.5`

### 执行

用 Bash 工具运行以下命令（保持参数顺序，问题用引号包裹）：

```bash
node .claude/skills/vote-sim/scripts/vote_sim.mjs $ARGUMENTS
```

如果 $ARGUMENTS 包含多个词，需要将问题部分用引号保护。若用户的原始输入中问题已有引号则直接传入；若无引号，将第一个空格前的内容当作问题用引号包裹。

脚本会自动完成：
1. 生成 N 个服从正态分布的温度值
2. 分批并发调用 API（默认2并发）
3. 模糊匹配投票结果
4. 输出总体票数、温度区间分析、正态分布直方图
